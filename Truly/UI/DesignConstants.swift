import CoreFoundation

/// Shared layout constants used across multiple views.
enum DesignConstants {
    /// Corner radius for notification-window picker rows (OnboardingView + SettingsView).
    static let windowPickerRadius: CGFloat = 16
}
